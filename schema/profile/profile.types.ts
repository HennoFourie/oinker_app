export type schema = {
    key: string,
    displayName: string,
    bio: null | string,
    coverImg: string,
    profileImg: string,
    followerIds: string[],
    followingIds: string[],
    oinkIds: string[]
}